# Removing a spot

Prefer setting `published=false` first so the entry disappears from the public guide while retaining its record for review. Delete only when the record is genuinely no longer needed.