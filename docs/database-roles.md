# Database roles

`anon` and `authenticated` can select published spots. Spot mutations require authenticated admin membership. Recommendation inserts are public but recommendation reads are admin-only.