# Photo workflow

Upload permitted images to the Supabase `spot-images` bucket and store the resulting public URL in `spots.image_url`. The UI automatically replaces the genre placeholder with the image.