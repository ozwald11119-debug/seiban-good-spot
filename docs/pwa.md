# PWA

The service worker caches the application shell. Guide content itself is fetched from Supabase when available, while a small built-in fallback keeps the basic guide usable during an API outage.