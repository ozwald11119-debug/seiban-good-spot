# Troubleshooting

If the site shows fallback data, check the Supabase project health and browser network request to `/rest/v1/spots`. If deployment fails, inspect the latest GitHub Pages workflow run on `main`.